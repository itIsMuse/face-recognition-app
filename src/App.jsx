import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <div>
      {/* install those packages on your chrome book 
      1. particle-bg 
      2. parallax-tilte */}
    </div>
  )
}

export default App
